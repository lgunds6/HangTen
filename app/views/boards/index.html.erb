<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <title>Document</title>
</head>
<body>
    <div class="container mx-auto"></div>
<h1> Board Shop </h1>
</div>

    <% @boards.each do |board| %>
    <%= image_tag board.picture if board.picture.attached? %>
    <h3><%= board.title%></h3>
    <h5><%= board.condition%></h5>
    <h5><%= board.price%></h5>
    <%= link_to "View", board_path(board) %>
    <%= link_to "Edit", edit_board_path(board) %>
    <%= link_to "Delete", board_path(board) %>
<% end %>

<div class="flex space-x-4 ...">
    <div class="flex-1 ..."><h3><%= board.title%></h3></div>
    <div class="flex-1 ..."><h5><%= board.condition%></h5></div>
    <div class="flex-1 ..."><h5><%= board.price%></h5></div>
  </div>

  <!-- <%= yield %> -->
</body>
</html>

